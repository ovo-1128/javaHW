import java.util.*;
public class StringToNumber1{
  List<String>arr = new ArrayList<>();
  public int search(String find){
    list.addAii(arr,tmp);
    List<Integer> newList = new ArrayList<Integer>(strArrayList.size());
    for (String Int : strArrayList){ 
         newList.add(Integer.valueOf(Int)); 
        }
    return arr.indexOf(find);
  }
}